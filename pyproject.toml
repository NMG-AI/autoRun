[project]
name = "autorun"
version = "0.1.0"
description = "跨平台自动安装程序 - Tkinter GUI"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
